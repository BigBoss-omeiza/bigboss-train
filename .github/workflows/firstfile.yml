name: Build and Deploy Application

on:

  push:

    #branches: [ main, develop]
    
    #path_ignore:
    ##  - '**.md'
     # - 'docs/**'
  
  #pull_request:
   # branches: [main]
    #paths:
     
    #- 'src/**'
    #- 'package.json'
    #- 'package-lock.json'

  #workflow_dispatch:
    inputs:
      environment:
        description: 'where to deploy' 
        required: true 
        default: 'staging'
        type: choice
        options: [staging, production]
#env: 
  NODE_VERSION: '18'

jobs:
  test:
    runs-on: ubuntu-latest
  
    steps:

      - name : checkout code
        uses: actions/checkout@v4
      - name: test job
        run: echo "Testing .......... "

  security:
    runs-on: ubuntu-latest
    steps:

      - name : checkout code
        uses: actions/checkout@v4

      - name: security jobs
        run: echo "this ihe security job"
  
 # deploy:
   # needs: test
   # runs-on: ubuntu-latest

  #  steps:
  #    - run: echo "deploying application"






 